{
  "name": "readback",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "astro dev --host",
    "build": "astro build",
    "data": "node scripts/fetch-authoritative.mjs && node scripts/build-cameras.mjs",
    "authoritative": "node scripts/fetch-authoritative.mjs && node scripts/merge-authoritative.mjs",
    "news": "node scripts/fetch-news.mjs",
    "stage": "npm run build && wrangler pages deploy dist --project-name readback-bpq --branch staging --commit-dirty=true",
    "deploy": "npm run build && wrangler pages deploy dist --project-name readback-bpq --branch main --commit-dirty=true",
    "refresh": "npm run data && npm run news && npm run deploy",
    "og": "node scripts/make-og.mjs"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.7.4",
    "astro": "^5.0.0",
    "maplibre-gl": "^5.0.0"
  },
  "devDependencies": {
    "wrangler": "^4.128.0"
  }
}
